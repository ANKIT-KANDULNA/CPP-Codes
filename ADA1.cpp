#include <iostream>
using namespace std;
struct Pair {
    int min;
    int max;
};
struct Pair fun(int arr[], int low, int high) {
    struct Pair SP;
    if (low == high) {
        temp.min = arr[low];
        temp.max = arr[low];
        return SP;
    }
}